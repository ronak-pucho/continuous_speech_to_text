## 0.0.2

* Fixes a bug where `onEvent` method was missing from the `RecognitionListener` implementation on Android, which caused build failures.

## 0.0.1

* Initial release.
* Adds continuous speech-to-text recognition on Android.
* Supports custom locales and silent background restarting.
